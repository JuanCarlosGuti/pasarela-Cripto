/**
 * Adaptadores del contexto de liquidaciones: entrada (REST) y salida
 * (persistencia JPA). Implementan los puertos definidos por el dominio.
 */
package com.pasarela.liquidaciones.infraestructura;