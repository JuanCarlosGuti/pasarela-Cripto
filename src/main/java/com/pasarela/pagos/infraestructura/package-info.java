/**
 * Adaptadores del contexto de pagos: entrada (REST, webhooks) y salida
 * (persistencia JPA, adaptadores de proveedores como BinancePayAdapter).
 * Implementan los puertos definidos por el dominio.
 */
package com.pasarela.pagos.infraestructura;